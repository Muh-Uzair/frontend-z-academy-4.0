import React from "react";

const Header = () => {
  return (
    <header className="h-14 bg-primary text-primary-foreground flex items-center px-6 border-b fixed left-0 right-0 top-0 h-[50px] z-100">
      <h1 className="text-lg font-semibold">Dashboard</h1>
    </header>
  );
};

export default Header;
